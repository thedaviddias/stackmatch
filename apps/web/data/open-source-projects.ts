/**
 * Re-exports from @stackmatch/config — single source of truth for project data.
 */

export { openSourceProjects } from "@stackmatch/config/projects";
export type { OpenSourceProject } from "@stackmatch/types/project";
