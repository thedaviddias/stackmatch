/**
 * Re-exports from @stackmatch/seo — single source of truth for SEO metadata.
 */
export {
  canonicalUrl,
  createDynamicMetadata,
  createMetadata,
  createWebPageJsonLd,
  createWebSiteJsonLd,
  DEFAULT_KEYWORDS,
  formatTitle,
  INDEXABLE_ROBOTS,
  NOINDEX_ROBOTS,
  rootMetadata,
} from "@stackmatch/seo";
