/**
 * Re-exports from @stackmatch/config — single source of truth for design themes.
 */

export { DEFAULT_THEME, DESIGN_THEMES, THEME_STORAGE_KEY } from "@stackmatch/config/themes";
export type { DesignThemeId } from "@stackmatch/types/theme";
